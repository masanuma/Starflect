# Starflect 残タスク・アイデア

> このファイルはバックログの保管場所です。チャット履歴に依存せず残すため Git 管理下に置いています。
> 着手したら該当項目にコミットハッシュや日付を書き添えてください。

最終更新: 2026-07-28（憲法に沿った全体整備・ローカル開発の詰まり解消）

---

## 🧭 コンセプト憲法（2026-07-27 改訂・迷ったらここに戻る）

> ### 一行
> **普通の占いより精度が高い。なぜなら10天体を見ているから。**
>
> | | |
> |---|---|
> | **目的** | **精度**。当てもの一発ではなく、その人だけに合った深さ |
> | **IF（窓口）** | **ほしキャラ**（16種）。複雑さを1つの顔に畳むための発明 |
> | **主役の体験** | **10天体を理解したAIが相談に乗る**。ここが売り |
> | **10天体の説明** | 「**なぜならば**」。**興味がある人だけ**が見る。教える必要はない |

### 出発点（忘れないこと）
**10天体の精度の高い占いを作りたかった。でも複雑なので、IFとしてほしキャラを用意した。**
＝ **10天体を"教えたい"のではない**。精度を届けたいだけ。

### やりがちな間違い（実際にやった）
「10天体をわかってもらおう」として、**説明ページを作り・新語を作り・分類を増やした**。
→ 教える必要がないものを教えようとすると、複雑さが再生産される。
**「説明が要る」と感じたら、説明を足す前に"表に出す量"を減らせないか考える。**

### 「毎日ひらく」と「AIを使う」は同じ課題
> **毎日開く理由 ＝ 相棒に話しかける理由**

毎日用の別機能を作るのではなく、**AI相談そのものを毎日のループにする**。
先の期間の情報を先出しすると、**明日来る理由もAIに聞く理由も同時に消える**（＝運勢カード6期間タブの失敗）。

### 既知の憲法違反（気づいたときに直す）
- [ ] **運勢カードの本文が専門用語のまま** ← 最大の違反。毎日必ず目に入る面なのに
  「運行中の太陽 × あなたの木星(発展) — 大きな追い風(120°)」と読み上げている。
  AI相談室は日常語に直した（`2026-07-27`）が、このカードだけ取り残されている。
  直すなら「いまの巡りが、あなたの発展担当に追い風」のようにAIと同じ言い換えルールを適用する。

### 判断のしかた
新しい要素を足すとき、必ず問う：
1. **これは精度に効くか？**（効かないなら足さない）
2. **これはAI相談に人を運ぶか？**（遠ざけるなら置き場所を疑う）
3. **これは「なぜならば」か？**（＝畳んで、見たい人だけに）

### AIの答え方（コンセプトの実装そのもの）— `2026-07-27 実装済み`
- **精度は落とさない**：AIには**全11天体＋全期間**を渡す。変えるのは**出力の見せ方だけ**
- **基本は日常語**。星の名前は**1回の返答に最大1つ**
- 運行データ「運行中のA × あなたのB」は**そのまま2つ並べない**。運行側は「いまの巡り」とまとめ、**出生側の役割名だけ**出す（例:「いまの巡りが、あなたの愛情担当に追い風」）
  - ※ルールを強めるだけでは星3個→2個止まりだった。**具体的な言い換え方**を示して初めて0個になった
- **深掘りを求められたときだけ**、天体名・星座名・角度をきちんと出す

---

## ⚠️ 重要な技術的注意（同じ轍を踏まない）
- **サーバー(server/)から astronomy-engine を import しない**。`signs.ts`/`fortune.ts`/`planets.ts`/`astro.ts` は astronomy-engine を名前付きimportしており、Railway の **Node 18** ではESM解決に失敗して**起動クラッシュ**する（"does not provide an export named 'Body'"）。天体計算はクライアント専用。
  - サーバーがキャラ名/エレメント等の言語別データを使うときは、astro非依存の `src/lib/starData.ts`（STAR_TYPES / ELEMENT_LABEL / ELEMENT_WORD_L）を参照する。`signs`/`startypes` を値importすると astro が芋づるで載るので注意。 — 事故と修正: `247ec68`（2026-07-23、本番ダウン→分離で復旧）
- **ローカル検証**: Expressサーバーは `dist/index.html` の更新時刻を見て自動で読み直す（`c1fe55a` 以降）。**サーバーを起動したまま再ビルドしてOK**。
  - それ以外（`server/` や `src/lib` のソース変更、LP・`/stars` の文言）は**再起動が必要**（tsx はホットリロードしない／静的ページは起動時に生成）。
  - Vite dev(5173) は Express 製の静的ページ（`/`・`/stars`・`/c/<slug>`）を配信しない。それらを見るときは `full` 構成（`npm start`）を使う。

---

## ✅ 完了
- **コンセプト憲法に沿って全体を整備** — `e8a4147`＋`c1fe55a`（2026-07-27）
  - **AIを翻訳者にした（声の層）**：日常語で答え、**1回の返答に出す星の名前は最大1つ**。深掘りを聞かれたときだけ天体名・星座名・角度を出す二段構え（実機検証で普段は星名0個／深掘り時は角度まで）
    - ⚠️ **要点**：ルールを強めるだけでは星3個→2個止まりだった。運行データは「運行中のA × あなたのB」で渡るため**そのまま2つ並べてしまう**。「運行側は"いまの巡り"とまとめ、**出生側の役割名だけ**出す」という**具体的な言い換え方**を示して初めて0個になった
    - 精度は不変（AIには全11天体＋全期間を渡す）。役割名も渡すようにした
  - **運勢は「今日」だけにし、先の期間はAIへ渡す**（ソロ・相性とも）。6期間タブを廃止し［明日は？］［今週は？］［来月は？］で相談室へ自動スクロール＋送信。**先出しは「明日また来る理由」と「AIに聞く理由」を同時に奪っていた**。見出しも「今日の運勢」に
  - **新語「かけらぼし」を廃止**（残存0）。隠すと決めたものに覚える名前を付けるのは矛盾だったため素直な「星」に戻し、内訳は3行（自分のこと／人とのこと／時代のこと）に畳んで行ごとに開く
  - **LPを憲法どおりに書き換え（7言語）**：ヒーローで精度訴求「太陽星座だけじゃない。10天体で読む、あなただけの16キャラ占い。」／「ほしキャラとは？」に「裏で全10天体を読む・むずかしい話は表に出さない」段落を追加／**使い方③が「今日〜来月の運勢」と事実と食い違っていたのを「ほしキャラに相談」に修正**
  - **総合診断の1文目**で星座名が手がかりなく出ていたのを「**見た目担当（いて座）の『自由』**」形式に（役割名を主・星座名を従）。3文目は元から手がかりありのため変更なし
- **ローカル開発の詰まりを2つ解消** — `8301d61`＋`63f5417`（2026-07-28）
  - `dist/index.html` を**更新時刻で読み直す**ようにし、**サーバーを起動したまま再ビルドしてOK**に（従来はキャッシュが消えたバンドルを指して /app が起動しなかった。今日2回踏んだ）
  - Expressサーバーが **`.env` を読む**ように（Viteは読むがサーバーは読まず、`npm start` だけAIが「APIキー未設定」だった）。既存の環境変数は上書きしないので**Railwayの Variables が常に優先**。`.env` を退避して起動し、全ルート200＋AIは未設定エラーを返すだけで落ちないことを実測
- **10天体と12星座の説明ページ `/stars` 新設＋「かけらぼし」命名＋ユーザー指摘対応** — `c18b604`→`d630112`→`2a99bed`（2026-07-27）
  - **ユーザー指摘の対応**：①星座名を日本語のみ**ひらがな表記に統一**（「蠍」等が読めない方が一定数いる）。`signs.ts` の1箇所でアプリ全体に反映＋AIプロンプトの翻訳ヒントも「しし座→Leo」に更新（揃えないとAIの対応表がずれる） ②かけらぼし行の本文を左揃え＋特徴の「」除去 ③静的ページの説明文（`section .lead`／ヒーロー）を左揃えに統一（LP・キャラ別にも適用）
  - **`/stars`（7言語・sitemap 119→126URL）**：**結果の読み方**（天体＝誰が × 星座＝どんなふうに／`roleSign` を再利用してアプリと同じ組み立てを例示）→ **太陽を単独で頂点**（雑誌の12星座占いの「あなたの星座」＝これ、と接続）→ **3グループ** → **12星座をエレメント別4×3**。天体は `PlanetMascot` をSSRしてアプリと同じ顔・天体色
  - **「パーティ」表現を全言語で廃止**。RPG感が突然でアプリのやわらかい語彙から浮いていたため。「星キャラ」は16タイプの「ほしキャラ」と紛らわしく不採用 → **「かけらぼし」**を採用（カード見出し「あなたをかたちづくる、11のかけらぼし」）
  - **分類をアプリと `/stars` で共通化**：太陽を先頭に **毎日のあなた担当**（月・上昇・水星・金星・火星）／**人づきあい担当**（木星・土星）／**時代まるごと担当**（天王星・海王星・冥王星）。根拠は公転の速さで、各グループに移動スピードを添え、世代天体は「友だちと冥王星が同じなのはこのため」まで書いた。グループ名は `ui.ts` に集約し2画面で共有。カードはグループ単位で畳む（途中で切らない）
  - **相棒ホームの情報整理**：今日の運勢を一等地へ。地図はLv進捗だけ残して宝箱を畳む（運勢の開始位置 1035px→**266px**、全体 2933px→**2527px**）
  - **結果画面**：キャラ→かけらぼし→シェア→運勢→相談室 に並べ替え（納得してからシェアに出会う流れ）＋初回だけ「**この先の楽しみ方**」案内カードで相談室へ誘導
  - ⚠️ **再発防止**：`planets.ts`/`signs.ts` は astronomy-engine を読むためサーバーから使えない。表示用データを astro非依存の **`src/lib/astroText.ts`** に抽出（`starData.ts` と同じ役割分担）。マーカーで**サーバー起動時の astronomy-engine 読み込み0回**を実証済み
  - ⚠️ **反省**：`d630112` でカード見出しだけ改名し、**ごほうび地図の宝箱名／初回案内の本文／`/stars` の導入文**に「パーティ」が全言語で残っていた（`2a99bed` で修正）。**表示文言の一括変更時は必ず全言語×全画面を grep すること**
- **診断・相性の結果に「わっ」のリビール演出** — `73f5661`（2026-07-27）。ソロ=タメ→太陽と月が寄って並ぶ→ほしキャラ誕生(バネ)→名前／相性=タメ→ふたりのキャラが出会う→相性を0から数え上げ→ニックネーム。どちらも約3.6秒・タップでスキップ可・同じベールと間合い。余白も調整(マスコット96→150px、名前拡大、太陽×月の式を直後に、長文と総合診断は点線の下へ)。再生はソロ=初回診断のみ／相性=初めての組み合わせのみ、`prefers-reduced-motion`では再生しない。文言7言語追加
  - 実装中に潰した罠（再発防止）: ①ベールのフェードイン中に結果が透けてネタバレ→**最初のフレームから不透明＋演出中は結果を `visibility:hidden`**（実測で漏れ0フレーム）②状態クラス `reveal-*` が中身の `.reveal-reading`/`.reveal-name` と衝突しオーバーレイ自身が点滅・移動→**状態は `is-*` に分離** ③演出終わりに一瞬の空白→**カードの立ち上がりをベールのフェードより一拍早く**（3320ms／3600ms）
- **相性のAIを相談室と同じチャット形式に＋入力期間選択を削除** — `92d272e`（2026-07-27）。一発AI鑑定→ストリーミングチャット(文字入力/履歴/スターター、ふたりの相性専門の人格)。`/api/ai-pair-chat` 追加、ストリーム処理を `createStreamingChatHandler` に共通化(相談室/相性で共用)、`AiChat` を汎用化(stream/copy/headerIcon 上書き・後方互換)。相性フォームの期間選択は結果画面で切替可のため削除。文言7言語追加。実API検証済み。※旧一発鑑定コード(`AiReading`/`src/lib/aiReading.ts`/`/api/ai-pair`)は未使用のまま残置(別途掃除可)
- **相性(ペア鑑定)の改善3点** — `67bc4df`（2026-07-25）。①1人目=自分を診断済み情報(`starflect-input`)から自動プリフィル ②結果画面に期間タブ(今日/明日/今週/来週/今月/来月)追加＝全てローカル計算でAIコストなし ③個人名に敬称「さん」(日本語のみ)を表示・ふたりへのヒント・AI鑑定へ一貫適用（二重敬称回避）。あわせてAIチャット/発見レポートの敬称も言語対応化(ja=さん・他言語=なし)。運勢の期間タブ定数を fortune.ts に共有化。実API(相性・チャット)でローカル検証済み
- **相棒ホームに10天体パーティを追加** — `73eeadb`（2026-07-24）。結果画面のパーティを `PartyCard` 共有部品化し、2回目以降の相棒ホームでも常時確認可（地図の下・デフォルト畳み込み「◯人のパーティを見る」で全11体展開）。従来は「自分の情報を変更する」経由でしか見られなかった売りの10天体を常時アクセス可能に。partyReveal を7言語追加
- **紹介LP/16キャラページを7言語化＋コンテンツ整理を本番リリース** — `c5fc199`＋`247ec68`（2026-07-23）
  hreflang・言語スイッチャ・sitemap 119URL、エレメントマーク掲載、CTA統一、FAQ/説明/16キャラ紹介をLPに一本化、
  アプリのFAQ撤去・About画面廃止(→LP)、FAQ「無料」表記を全言語除去、LP→アプリの言語引き継ぎ(?lang=)
- スクロール最上部表示の修正（画面遷移で `window.scrollTo(0,0)`） — `35b2d46`
- 逆行サフィックスの7言語対応（仏・伊・葡・韓を追加） — `aaaaf8a`
- フィードバック専用アイコン（ハート＋きらめき）を追加し相談室と差別化
- **SEO/AIO の基本整備** — OGP/canonical/robotsメタ・JSON-LD・sitemap.xml・robots.txt・favicon・
  クローラー向け静的本文／FAQ(7言語)＋FAQPage構造化データ・llms.txt・AIボット許可明示
- **Google Search Console 登録**＋sitemap.xml 送信＋インデックス登録リクエスト（ライブテストで「登録可」を確認済み）
- **⭐ 星の相棒（ほしキャラ常設化）v0.1 を本番リリース** — `9735d58`（2026-07-21）
  22コミット／16ファイル／+1765行。詳細は下記セクション
- **⭐ 星の相棒 v0.2 STEP1「ごほうび地図」を本番リリース** — `4556b57`（2026-07-22）
  縦ロードマップ＋累計理解度(Lv)で「使うほど占いが自分仕様に深化」を可視化。詳細は下記セクション
- 結果画面のパーティ行を「◯◯の△△星座は□□」の一文化（役割×天体×星座を色分け）／逆行バッジを記号「℞」→「逆行」テキストに
- **AI相談室のトランジット全期間対応** — チャットに今日/明日/今週/来週/今月/来月＋この先数か月を渡す（`forecastSet`）。「今週/今月のデータが無い」致命バグを解消 — `79c48ea`
- **AI鑑定モデルを Opus 4.8 → Sonnet 5 に統一**（chat/pair/report＝`AI_MODEL`）。コスト約4〜6割減。運行の星名も列挙どおり厳守ルール追加。実データで忠実性を本番検証 — `3b9cc80`
- **⭐ 結果のSNSシェア導線 Phase 1 を本番リリース** — `e964bf0`（2026-07-23）
  ネイティブ共有/X/LINE/コピー。キャラ名入り定型文7言語。共有URL=自分のオリジン+?c=slug（受け取った人が自分の診断に入る動線＋将来のOGP出し分けの布石）。GA4 `share` イベント

---

## 🌟 プロダクト方針：毎日会いにいく「星の相棒」（**v0.1 本番稼働中**）
> 背景：数日使った実感として「パッとしない／ときめかない／普通の占いより地味」。
> 原因は **一発診断で終わり毎日の理由がない・結果が“報告書”・長文で快感の型がない**こと。
> 方針を **「診断ツール」→「毎日会いにいく“星の相棒”」** に転換する（方向性A、採用決定）。

### 核となる差別化：相棒育成ループ（“当てもの”を超える）
普通の占いは答え合わせをしないので「ただの当てもの」で終わる。そこを閉じて**関係性**にする。
ただし **“的中の採点”にはしない**（大半が外れて信頼を損なう＆景表法の優良誤認リスク）。
→ **答え合わせ＝対話と記録**。相棒はドヤらず「昨日どうだった？教えてくれてありがとう」と受け止め、
明日の語りに織り込む。価値は“当てる”ではなく **“わかってくれてる”**。使うほど蓄積し、
**あなた専用に育つ＝乗り換えられない**（本格星読み×AI×キャラ人格＝うちだけの武器）。

### 確定した設計判断
- **相棒＝あなたのほしキャラ**。診断で出た16タイプのキャラ（例：静かな火山）が、静的な結果ではなく
  **人格を持って毎日語りかける“生きた相棒”に昇格**する。所有感とブランドの一貫性を両取り。
- **“たまごっち感”は採用、ただし義務・罰・死は排除**。サボっても責めない／死なない／減点しない。
  放置しても関係は途切れず、戻れば温かく迎える（「おかえり」）。育つのは一方向（貯まる・深まる、減らない）。
  ＝安心して離れられる育成。メンタルケア用途で罪悪感メカニクスは毒。「また会いたい」は残し「会わなきゃ」は消す。
- **振り返りはタップ主体で軽く**（`いいことあった😊 / 微妙😔 / ふつう` 等）。ひとこと日記は任意のオマケ。
  - 理由：**日記を毎日書ける人はもうAIに相談している**。面倒だと人間は続かない。軽さ最優先。
- **細かい当たり外れは読まない**。むしろ **継続性からの計測**を主役に
  （例：タップの蓄積から「最近、気分が晴れないみたい」を相棒が気づく＝トレンド検出）。
- 占いは**“予言”から“朝の問いかけ＋夜の受け止め”**へ。星は会話のきっかけ、**商品は関係性**。

### 信号の経済（入力設計）＝「毎日＋簡単フィードバック」でどこまで“されていく”か
ほぼ手間ゼロの信号から、どれだけ豊かに導出できるかの設計。相棒は“育てる”ではなく痕跡から“育っていく”。

| 信号 | 手間 | 導出されるもの |
|---|---|---|
| 出生図（初回1回） | ゼロ | 相棒の素の人格・表の顔。Day1から効く土台 |
| 訪問の時間帯・曜日 | ゼロ(自動) | 生活リズム／朝型夜型／週末の落差 |
| 開く頻度・間隔 | ゼロ(自動) | 波を拾う(罰にしない)。「久しぶりの日」を温かく迎える材料 |
| よく触るテーマ | ゼロ(自動) | いまの関心(恋愛ばかり開く＝頭がそこにある) |
| 気分タップ 😊😔ふつう | 1タップ | 気分トレンド・移動平均 |
| 2タップ目「何系？」 | +1(任意) | 気分×領域(仕事の日に😔が多い) |
| 一言への反応(刺さった/ピンとこない) | 1(任意) | 何が本人に響くか＝人物像のチューニング |
| 相棒の「1日1問」遊びの二択(スキップ可) | 1(任意) | 心理プロファイルを“質問感なく”蓄積 |

- 明示的に聞くのは**1日1問まで・遊びの二択・スキップ可**。アンケートでなく“相棒があなたに興味を持ってる”演出。
- **隠れた自分エンジン**：本当の魔法は「聞かずに計算で出す」。
  - 気分 × トランジット相関 →「土星が絡む日にしんどくなりやすいみたい」＝あなた専用に調律された占い（他社が出せない必殺技）。
  - **出生図(アーキタイプ＝建前) と 日々の行動(実測) のギャップ**＝隠れた一面の正体。「表の顔は社交的なのに、ひとりの日に元気が戻ってるね」。星が素材、ギャップが商品。
- 到達目安：near-zero の手間でも **2〜3週で「わかってくれてる」に到達可能**。Day1〜3は出生図頼みで温かく → Week2〜3で“見抜き”が発火。
- 注意：小サンプルはノイズ。相関は断定せず「〜かも」トーン厳守（信頼＆景表法）。

### 相棒の声・トーン
- **“気持ち悪い寄り添い”を消す**。空虚な肯定の乱発（おべっか）はしない。悩んでいる人に刺さるのは、肯定ではなく
  **やさしい指摘・正直なフィードバック**。最初は多少の寄り添いはOK、でも core は“やさしい本音”。
- **星が“優しい本音”の乗り物**：批判や注意を「私が言う」でなく「**今の星の運行がこう出てるから**」に載せる。
  角を立てず、おべっかにも説教にもならずに背中を押せる。三点セット＝**温かさ＋正直さ＋星の具体**。
- **軸はあくまで星座占い**。今のトランジットから具体に落とす：「こういう傾向だから、ここは注意、ここは思い切って大胆に」。
  エッセンスを忘れない。占いの役割＝**背中を少し押して自信をつける／そっと注意する**。断定予言・不安煽りはしない。
- **距離感は「昔からの友人」**。ブランクがあっても責めず重くせず、「最近どうしてた？」で自然に再開。
  親密さは実際の利用履歴に合わせて調整（会ってない期間はそれ相応の距離で、でも温かく）。

### 相棒の正体・語り口（7日間ペーパーシミュで確定）
- **正体＝あなたのほしキャラ本人**（例「静かな火山」）。**あなたと同じ星でできた“同族の相棒”**。
  「もう一人のあなた」宣言はしない（気持ち悪い）。外から観察する助言者にもしない（監視感で怖い）。
- **一人称「わたし」**。自分のほしキャラ名をセリフで毎回名乗らない（肩書き表示で担保）。
  型の話は所有格でなく **「わたしたち、こういう日ほど…」と同じ側から**（例：「ミオの静かな火山は」はNG＝関係が不明瞭）。
- **メタ宣言をしない**：「当てにいかない」「隣で見てる」等は口に出さず態度で示す（宣言は逆に監視・恐怖を生む）。
- **星は「星の巡り／今日の星」**と言う。「空の様子」＝天気予報っぽいのでNG。
- **名前呼びはオプション（デフォルトは「あなた」）**。獅子座の月等には効くが多用は重い。使うなら再会・特別な瞬間だけ。頻度は別途単体で検証。

### 未来と振り返りのバランス（＝占いの本分を忘れない）
- 占いの本分は**未来予測**。振り返り（答え合わせ）は関係を作る装置だが、**ウエイトはむしろ未来側**でよい。
- 役割分担：**朝カード＝“今日”の未来（追い風／注意）／夜タップ＝振り返り（短く・見抜きの燃料）／週末＝“翌週”の未来予測**。
- **週末は「今週こうだったね」で終わらせない**。翌週の運行から **よさそうなこと（追い風）＋注意すること** を必ず出す。
  振り返りは3行程度に圧縮し、尺は未来に割く。→ 「来週こういう追い風」が**次に開く動機（楽しみ）**になる。

### 1日ループ（たたき台）
- 朝：相棒が“今日の星”を読んで一言＋そっと注意/追い風（未来向き）。今日のカード（色・キーワード）。
- 夜：「今日はどうだった？」を軽タップ（😊/😔/ふつう ＋任意で「何系？」）→ 相棒が受け止め、記憶に刻む。
- 週末：**圧縮した振り返り → 翌週フォーキャスト（追い風＋注意）**。未来にウエイト。
- 蓄積 → 仲良し度、語り口が親密に、過去を参照。“見抜き”は毎週やらず**たまに・不意に**（驚きの持続）。

### 検証済み：7日間ペーパーシミュ（架空ペルソナ「静かな火山」）
- Day1出会い→Day4に1問の伏線→Day7で回収、の弧が効くと確認。**Day4伏線→Day7見抜き**が“見られてた驚き”の主役。
- ②“見られてた驚き”と③“覚えててくれる関係”は蓄積前提＝**Week1後半で本領**。
- 最初の関門＝**Day1〜3をどう飽きさせず持たせるか**（朝カードの報酬＋出生図由来の「あなただから」の具体で持たせる）。
- 注意点：Day7の見抜きは長文になりがち→短く。見抜きは頻発させない。

### 実装の勘所（既知で解ける）
- **記憶の圧縮**：全履歴は毎回渡せない。定期的にAIが「この人プロフィール」を1枚に要約して保持（長期記憶チャットの定番）。
- **コスト**：日常の一言は安いモデル/テンプレ、要所だけ上位モデル。→ 「AIコスト対策」と直結。
- **データ永続化／引き継ぎ**：基本はローカルで育成（プライバシー＆軽さ）。機種変対策は
  **LINE風の「いったんクラウドへアップ → 新端末でダウンロードして復元」**方式（引き継ぎコード等）。
  フルログインを必須にせず“安心感”を出す。将来サブスク化のタイミングで正式アカウントに発展。
- **プライバシー**：良かった/嫌だったの記録＝センシティブな生活データ。当面ローカル完結を徹底、同期するなら同意と保護必須。

### 次プランで詰める設計論点
- **相棒との“最初の出会い”**（Day1の「今日から一緒に星を見ていくね」という契約シーン）の設計。ここが弱いと結果画面のままになる。
- **夜タップの粒度**：`😊 / 😔 / ふつう` だけか、**2タップ目で「何系？（仕事／恋愛／人間関係／なんとなく）」**を足すか。
  足すとトレンドが「最近“仕事”で😔が続いてるね」まで言える。手間とのトレードオフ。
- **気分トレンド検出**：タップ蓄積から「最近、気分が晴れないみたい」等を相棒が気づくロジック（移動平均・連続日数など）。
- **UXシミュレーションの進め方**：架空ペルソナで7日間を文言・タップ選択肢まで書き起こす（設計検証が速い）／
  本人テストプレイは深いが実時間で1週間かかる。まずは架空ペルソナ想定。

### ✅ MVP v0.1（A1＋B1／日常はテンプレ・AIゼロ）— 本番リリース済み `9735d58`
狙い：**毎日「朝の星＋夜タップ」ループでリテンション（また明日開く）が動くか**を最小コストで検証。

**実装した機能**
- **相棒ホーム**（2回目以降の起点）：罰なし挨拶（経過日数で出し分け）＋運勢＋相談室＋夜タップ＋週末まとめ。
- **ほしキャラが読む運勢**（`StarReading` 共有部品）：**今日/今週/今月・明日/来週/来月**を画面遷移なしで切替
  （来週=+7日、来月=翌月1日で `readFortune`）。**ラッキー/注意/注目バッジ**（アイコン＋文字）、**今日のラッキーカラー**（色名つき）。
- **夜の振り返りタップ**：😊😐😔＋任意で「何系？」→ テンプレで受け止め。同日は再質問しない。
- **週末まとめ＋翌週フォーキャスト**：直近7日の集計（簡易“見抜き”）＋来週の追い風/注意。土日表示。
- **データ**：`starflect-companion:v1 = { chart, starType, createdAt, lastVisitAt, daily{ 'YYYY-MM-DD':{ mood, domain?, forecastSeen } } }`（ローカルのみ）
- **GA4**：`companion_open` / `tap_mood` / `tap_domain` / `weekend_view`

**実装中に確定した追加の設計判断（重要）**
- **AIは「ほしキャラ相談室」に一本化**。別人格「AI占星術師」（ソロのAI鑑定・`/api/ai-reading`）を**廃止**。
  相談相手＝**あなたのほしキャラ本人**（システムプロンプトを本人設定に。一人称「わたし」・丁寧口調・「AI/占い師」と名乗らない）。
  → 表層＝ほしキャラが読む運勢（無料・テンプレ・一瞬）／深層＝同じほしキャラと相談室で会話（AI）の2層。
- **キャラは1つ＝あなたのほしキャラ**。「相棒」という別呼称もUI文言から排除して「ほしキャラ」に統一。
- **迎え入れボタンは廃止＝自動で相棒になる**。診断した時点で保存し、次回から相棒ホームが起点（「毎日そばに」を挙動で体現）。
- **診断は初回のみ**：データ無→診断カード／データ有→「あなたのほしキャラ「◯◯」と会話する」＋小さく「自分の情報を変更する」
  （フォームは `starflect-input` で前回値プリフィル済み）。
- **導線の出口/入口を両方確保**：相棒ホーム上部に「メインメニューへ」、ホームに相棒への入口。
- **表記・書式ルール**（今後もこれに従う）
  - ほしキャラ名は**必ず言語別の括弧**で囲む（JA「」/ EN "" / ES・FR・IT・PT «» / KO「」）。`quoted()` で一元化。
  - カードは**共通ヘッダー** `card-head`（ほしキャラのマスコット＋グラデタイトル18px/800＋説明）。
  - **説明文はセンタリングしない（左揃え）**。中央寄せは短い見出し・ラベル・設問のみ。
  - **カード間の余白は 16px 固定**（全カード `margin: 0 0 16px`）。
  - 見出し＝名詞句／ユーザーに回答を求める所だけ問いかけ（案C）。口調は**丁寧だが硬すぎない**で全画面統一。

### ステータス
- [x] UXシミュレーションで体験の芯を検証（7日間ペーパー）→ 声・トーン・未来ウエイト確定
- [x] MVP v0.1 実装 → 本番リリース（2026-07-21）
- [x] v0.2 STEP1「ごほうび地図」実装 → 本番リリース（2026-07-22 `4556b57`）
- [ ] **GA4でリテンション計測**（D1/D7・夜タップ完了率・週末まとめ開封率）。数日〜1週間ためて「また明日開くか」を判定 ← **次はここ**
- [ ] STEP1の予告宝箱の中身を埋める（下記 v0.2候補）

### ✅ v0.2 STEP1「ごほうび地図」— 本番リリース済み `4556b57`（2026-07-22）
狙い：v0.1に**「毎日開くと、いいこと（自己理解の深化）が待っている」の可視化**を足す。
「ログインボーナス＝鞭」に見せないため、**連続ではなく累計・関係でなく“理解度”**で語る。

**核心の再定義（ユーザーとの議論で確定）**
- 飴の中身は**ポイント/バッジ（外発的）でなく「使うほど自分がより深く・より“あなた個別に”わかる」（内発的）**。
- 数字は**「ほしキャラのあなた理解度：Lv.◯」**＝タップ/会話が「ほしキャラに自分を教えている」に見える。
  （相棒＝あなたを"知っていく"／親密度＝通った回数、ではない。後者は連続ログイン=鞭に転ぶ）
- キャッチフレーズ＝**「ほしキャラがあなたを知るほど、占いはあなただけのものになります。」**（地図の顔）

**実装した機能**（`RewardMap` ＋ `companion.ts` 拡張）
- **縦ロードマップ**：6段の宝箱を下から上へ。手に入れた＝点灯・タップで中身／先＝鍵つき予告（Lv.しきい値）。
- **累計理解度(signals)**：`CompanionState.signals`。減らない・罰しない。旧データは daily から補完。
  - 数え方（連打で稼げない）：**気分は「その日はじめて残したとき」+1／会話1往復+1／運勢閲覧は1日+1**。
- **中身の実装状況**：`誕生`（太陽×月）・`月星座の裏側`（月サインの素の顔）＝**実装済み**。
  `パーティの深掘り(Lv.10)`／`気分のクセ(Lv.25)`／`隠れた自分レポート(Lv.50)`／`ほんとうの相棒(Lv.100)`＝**予告のみ（枠だけ・中身は未実装）**。
- 「**理解度の上げ方**」の説明を Lv進捗直下に明示（運勢を見る／気分を残す／ほしキャラと話す）。
- しきい値は暫定 `3/10/25/50/100`（`MILESTONES` 定数、体感で調整前提）。7言語対応。口調は敬体で統一。
- ついでに**旧バグ修正**：`recordMood`/`markForecastSeen`/`touchVisit` を端末の最新state基点に（気分タップで運勢閲覧フラグが消える相互上書きを解消）。

### ✅ v0.2 STEP2「AI発見レポート」＋トランジット全期間化 — 本番リリース済み（2026-07-22）
- **AI相談室のトランジット致命バグ修正**（`79c48ea`）：チャットが1期間しか運行を持たず今週・今月以降を濁していた問題。
  `fortune.ts forecastSet()` で今日〜来月＋この先2〜3か月を一括計算（ローカル天体暦＝外部API不要）し、
  チャットに全期間を先渡し。プロンプトも「該当期間の実データで答える／『データが無い』と言わない／
  数か月〜先は遅い星の基調で語る」に修正。実機で今月・来年ともに具体回答を確認。
- **ごほうび地図の宝箱をAI生成レポート化**：`月星座の裏側/パーティの深掘り/気分のクセ/隠れた自分レポート` を
  **AIがその人の出生図×行動ログから専用生成**（`/api/ai-report`・opus・初回のみ生成→localStorageキャッシュ・
  開いたタップ時のみ＝コスト最小）。目玉の `隠れた自分レポート` は「星の建前×行動の実測ギャップ」を実際に指摘できることを実機確認。
  `誕生`＝テンプレ据え置き、`ほんとうの相棒(Lv.100)`＝準備中。
  - 背景：テンプレ1文では「深堀りされた感/特別感」が出ずインセンティブが薄い、という指摘への回答。

### v0.2 残（STEP2の続き）
- [ ] `ほんとうの相棒`（`Lv.100`）＝日常の一言のAI化／全ログ要約を踏まえた語り（記憶の圧縮とセット）
- [ ] さらに先の期間指定（例:「来年の夏」）のAI深掘り＝**AIが日付を要求→サーバが計算して返すツール実行**方式の検討
- [ ] **1日1問**の遊びの二択（質問感なくプロファイル蓄積）／仲良し度
- [ ] **クラウド退避→復元**（LINE風の引き継ぎ。機種変対策）
- [ ] 記憶の圧縮（プロフィール要約）＝長期運用の前提
- [ ] しきい値 `3/10/25/50/100` を実データ（GA4）で調整

---

## 🎨 デザイン改善フェーズ（課金の前提／2026-07-27 時点）
> **課金化の前にデザイン/体験を底上げする**と決めた（詳細は下記マネタイズ）。その進捗。

- [x] **結果画面の「わっ」の演出** — `73f5661`
- [x] **相棒ホームの情報整理**（運勢を一等地に・地図を畳む） — `c18b604`
- [x] **診断後の「まずはここから」導線**（初回だけ「この先の楽しみ方」案内→相談室） — `c18b604`
- [x] **用語と分類の統一**（かけらぼし／3グループ／`/stars`） — `d630112`
- [ ] **ほしキャラのビジュアル強化** — 16キャラの個性がもっと伝わる見せ方（未着手）
- [x] 運勢カードの圧縮 — 6期間タブの廃止（`e8a4147`）で短縮済み。これ以上の削減は**中身がユーザーの読みに来ている部分**なので当面やらない

---

## 🚀 成長ロードマップ（2026-07-23 検討で確定した方針）
> **順番の結論**：いまのボトルネックは「見つからない・広がらない」＝収益化ではない。
> **見つかる・広がる → 最初の流入 → 継続率(D1/D7)計測 → 収益化** の順。流入ゼロでは継続率も測れないので、拡大導線が先。

- **① SNSシェア導線**（拡大の起爆剤・最優先）
  - [x] Phase 1：ネイティブ/X/LINE/コピー＋キャラ名定型文（`e964bf0`）
  - [x] **Phase 2：キャラ別OGP画像**（16種+default）本番稼働（`a8d1484`）。`scripts/gen-ogp.tsx`（マスコットSSR→Zen Maru Gothic→resvg でPNG生成／`public/ogp/*.png`）＋サーバーが `?c=<slug>` で og:image・og:title を差し替え
  - [ ] （任意）OGP画像の多言語版（現状JA名のみ。非JA共有もJA名カードになる）
  - 補足：SNSはOGPを**キャッシュ**する。ルートURLの既存シェアは旧カードのまま出ることがある（X Card Validator等で更新可）。`?c=` 付き新URLは差し替え済み画像を取得
- **② 紹介LP＋16キャラ別ページ（サイト構造刷新・案B）＝本番稼働（`bf99e90`, 2026-07-23）**
  - [x] `/`＝紹介LP（静的HTML・SEO本体）。相棒があれば描画前に `/app` へ即リダイレクト
  - [x] `/app`＝アプリ本体（SPA・noindex＝検索重複回避）
  - [x] `/c/<slug>`＝16キャラ別ページ（読み物＋エレメント解説＋診断CTA＝シェア着地先）
  - [x] シェアURLを `/c/<slug>` に変更。sitemap に17URL登録。旧 `/?c=` はOGPのみ差し替えで救済
  - `server/characters.ts`（16キャラJAデータ）＋`server/pages.ts`（LP/キャラ別のSSRレンダラ、マスコットはReact SSR）
  - → 1ページSPAだったクロール面が**17ページに**。Googleに巡回理由を大量供給＝インデックス問題の根治
  - [x] **キャラ別ページ＋紹介LPの多言語化（7言語）** 本番稼働（`247ec68`, 2026-07-23）。hreflang・言語スイッチャ・sitemap 119URL・LP→アプリの言語引き継ぎ
  - [ ] （任意）相性の組み合わせページ等でロングテール拡張
- **③ AIコストの日次上限**（軽実装・保険）
  - 1日の無料AI回数に上限（例：チャット5往復/日・レポート1本/日、localStorageで軽く）。コスト暴走の保険＋将来の「もっと話したい→課金」境界を先に作る
- **④ GSC「URLが登録されない」＝技術問題ではない**（2026-07-23 実測で本番は 200/canonical/sitemap/robots すべて正常）
  - 原因：新規・低権威・1ページ＋**最初のクロール(2026/05/29)が404**→Googleが再クロールを強く後回し。ライブテストは「登録可」(緑)
  - 対策：ライブテストから**「インデックス登録をリクエスト」を押す**→1〜3週間待つ。根本改善は②（ページを増やす）
- **コスト単価の実測（Sonnet 5 導入価格）**：チャット≈1円/往復・レポート≈1.2円・相性≈1.5円。毎日運勢/診断/地図は**ローカル計算＝0円**
- **収益化は継続率が出てから**：**決定＝2ヶ月無料→月額¥480使い放題**（詳細は下記「マネタイズ」）。クーポン/チケットは不採用。有料化時は特商法表記必須
- **やらないこと（現時点）**：汎用アプリ紹介LP／課金実装／有料広告（継続率が出るまで）

---

## 🎯 マネタイズ（**方針決定・実装は先**／2026-07-27）
> **決定モデル：最初2ヶ月無料 → 月額¥480で実質使い放題（AI相談）**。年額割はユーザーが増えてから（テスト段階の今はやらない）。
> **課金化の前提＝デザイン/体験の底上げ**（「払う価値」を見た目・体験で納得させてから）。実装はデザイン改善のあと。
> 起動タイミングは **GA4でD7・継続率が固まってから**（順番＝拡大→継続率→収益化）。

- **なぜサブスク使い放題か**：AI1回の実測原価 ≈ ¥2.5（標準価格・`AI_USAGE`ログで実測）。「残り○回」チケットは「毎日話すほど深まる」核と真逆＝リテンションを削るので不採用。¥300/100回チケットはほぼ原価で利益も残らない。使い放題は損益分岐 月200回（ほぼ誰も届かない）で採算に余裕、法務も最シンプル（前払式支払手段の論点なし）。
- [ ] 実装時に付ける保険：**「常識的範囲で使い放題」＝ソフト上限**（例 50回/日）で異常利用・コスト暴走だけ止める。99%には使い放題のまま。
- [ ] 有料化時：**特商法に基づく表記**＋サブスクの**定期購入表示**（2022改正）／決済は Stripe 等に丸投げ（カード情報を自前で持たない）
- [ ] （将来）年額割（例 ¥4,800＝2ヶ月分お得）でLTV底上げ ← ユーザー増後
- [ ] （任意）投げ銭／サポート導線（Buy me a coffee 的な軽いもの）
- [ ] 広告は最後の手段（世界観を壊しやすい）
- ※チケット/クーポン制は不採用（前払式支払手段の論点＋上記の心理・採算の理由）。3ヶ月有効なら資金決済法は回避可だが割に合わない

## 💰 AIコストを Pay できる形にする工夫（マネタイズと両輪）
- [ ] **プロンプトキャッシュ**（Anthropic prompt caching）で入力トークン費を圧縮
- [ ] **鑑定結果のキャッシュ**（同一入力は再生成しない）
- [ ] **モデル選択** — 無料枠は `claude-haiku-4-5`、課金者は `opus`（DEPLOY.md:44 に約1/5コストと既述）
- [ ] **1日あたり無料回数の上限**でコスト青天井を防止

## ⚖️ 法令の確認ポイント（※専門家ではないので「調べるべき領域」の洗い出し。要専門家確認）
- [ ] **資金決済法** — 「AI利用クーポン」が*前払式支払手段*に該当するか。該当すると供託義務等が発生し得る。有効期限6ヶ月以内・自家型なら適用外の可能性など要確認
- [ ] **景品表示法** — クーポン/特典の表示（有利誤認・二重価格・優良誤認）
- [ ] **特定商取引法** — 有料化するなら「特商法に基づく表記」ページが必須
- [ ] **消費税・インボイス** — 課金時の対応
- [ ] **利用規約・プライバシーポリシー**の整備（生年月日＝個人情報。GA同意ゲートは対応済み）
- [ ] 有料「占い」表現と消費者契約法／景表法（優良誤認）

## 🎨 プロダクト改善
- [x] **結果のシェア機能**（X／LINE）＋**ほしキャラごとの動的OGP画像**（16種+default）— 上記①で本番稼働
- [x] **ほしキャラ別ランディングページ**でSEO流入 — 上記②（`/c/<slug>`）で本番稼働・7言語化済み
- [ ] **16キャラ図鑑ページ** — 「あなたは○番目」「レア度」等でコレクション欲を刺激。SEO入口にも
- [ ] **鑑定履歴の保存**（localStorage、将来はログイン）→ サブスクの価値づけにも
- [ ] **PWA化**（ホーム画面追加）でリピート率↑

## 🔍 SEO / AIO（基本の下ごしらえは実装済み。残りは手動対応）
実装済み(SEO): OGP/Twitter/canonical/robots メタ、JSON-LD(WebApplication)、robots.txt、
sitemap.xml、favicon、JS前クローラー向けの静的フォールバック本文（index.html）。
実装済み(AIO): 画面表示のFAQ（7言語・Home下部）＋FAQPage構造化データ、llms.txt、
robots.txt にAI回答エンジン（GPTBot/PerplexityBot/ClaudeBot/Google-Extended等）の許可明示。

- [x] **Google Search Console 登録＋sitemap送信＋インデックス登録リクエスト**（完了）。
  ※「GOOGLEインデックス」タブは再クロールまで古い404を表示し続けるが、ライブテストは「登録可」。数日〜2週間待ち。
- [x] **OGP画像（1200×630 PNG）作成**＋`twitter:card=summary_large_image` — 上記①Phase2で本番稼働（キャラ別16種+default）
- [x] **ほしキャラ16種の個別URL/LP** — 上記②（`/c/<slug>`・7言語）で本番稼働
- [x] **Bing Webmaster Tools** 登録＋sitemap再送信（2026-07-24）。※「検出URL」の反映は数日かかる。ChatGPT/CopilotはBingのインデックスを使うのでAIO的に重要
- [ ] （待ち）Bing の検出URLが **126** に増えたか、いずれ確認 ← 手動
- [x] **GSCでsitemap再送信＋代表5URLのインデックス登録リクエスト**（2026-07-24）。`/` は「登録されています」を確認
- [ ] （待ち）`/stars` を含む **126URL** 版で再送信し、追加ページの登録状況を見る ← 手動
